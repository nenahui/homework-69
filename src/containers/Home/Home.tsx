import { Typography } from 'antd';

export const Home = () => {
  return (
    <Typography.Text type={'secondary'}>
      Enter the show name in the input field and select the show
    </Typography.Text>
  );
};
